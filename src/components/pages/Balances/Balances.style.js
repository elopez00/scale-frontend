import { StyleSheet } from 'react-native'

export const style = StyleSheet.create({
    scroll: {
        minWidth: "100%",
        flexGrow: 1
    },
    scrollContent: {
        alignItems: "center"
    },
    headerButton: {
        fontSize: 25,
    }
})