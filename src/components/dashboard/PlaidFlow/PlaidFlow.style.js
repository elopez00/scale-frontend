import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    module: {
        height: "90%",
        minWidth: "80%",
        padding: 10,
        backgroundColor: "white",
        borderRadius: 20,
    },
});
